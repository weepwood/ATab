{
  "name": "atab",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "dev": "pnpm --filter @atab/extension dev",
    "dev:extension": "pnpm --filter @atab/extension dev",
    "dev:api": "pnpm --filter @atab/api dev",
    "doctor": "node scripts/doctor.mjs",
    "doctor:active": "node scripts/doctor.mjs --active",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test && node --test scripts/*.test.mjs"
  }
}
