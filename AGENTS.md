# AGENTS.md

本文件是 AI Agent 在 ATab 仓库中工作的强制入口。ATab 是浏览器标签页、原生书签、会话、云收藏、AI 操作与跨设备同步工作台。修改前必须先理解浏览器权限、数据事实来源、同步边界与 AI 执行模型，不得仅依据当前对话或局部代码直接扩展功能。

## 1. 修改前必读

按顺序读取：

1. 当前 Issue、PR 描述、验收标准和明确非目标；
2. `README.md`；
3. 架构、数据模型、AI 安全协议和部署文档；
4. 本文件；
5. 与修改模块相关的共享协议、数据库迁移、测试和同类实现。

如当前功能位于堆叠 PR 中，还必须读取全部上游 PR 的范围、未验证项和接口边界。

## 2. 项目边界

ATab 采用本地优先架构，核心边界包括：

- 浏览器扩展：Manifest V3、Vue 3、TypeScript、Vite、CRXJS；
- 本地数据：Dexie / IndexedDB；
- 浏览器事实来源：`chrome.tabs`、`chrome.bookmarks` 等浏览器 API；
- 服务端能力：Fastify API、AI Provider、Supabase Auth 与 PostgreSQL；
- 共享协议：扩展、服务端和数据库之间的结构化契约；
- AI 模型只生成计划，浏览器扩展负责最终校验、确认与执行。

禁止因局部需求建立第二套书签事实库、会话模型、同步协议、Provider 协议、权限系统或错误处理机制。

## 3. 风险等级

开始实现前声明风险等级：

- **P0**：大规模数据丢失、账号或同步系统泄露、危险浏览器操作绕过确认；
- **P1**：浏览器权限、书签删除、会话恢复、云同步、认证、RLS、数据库迁移、AI 写操作；
- **P2**：重要功能、跨包改造、共享协议变更、性能或 Chrome 109 兼容风险；
- **P3**：低风险 UI、文档、测试和内部整理。

P0/P1 必须有关联 Issue、失败路径测试、独立审查和可执行回滚。

## 4. 强制工作流

### 4.1 调查

先只读并输出：

- 相关包、文件和调用链；
- 浏览器 API、Dexie、服务端、数据库和共享协议影响；
- 已有可复用机制；
- 已确认事实、推断和未知项；
- 最小修改范围、风险与验证计划。

### 4.2 计划

计划必须写明：

- 每步修改的文件或模块；
- 为什么在这里修改；
- 如何验证本地、浏览器、服务端和数据库行为；
- 哪些相邻问题不处理；
- 如何独立提交和回滚。

### 4.3 小范围实现

- 一个 PR 只解决一个主要问题；
- 不混合功能、无关重构、依赖升级和全局格式化；
- 不为未确认的未来需求提前抽象；
- 发现相邻问题时建立 Issue；
- 提交说明使用中文；
- 默认从最新 `main` 创建分支并通过 PR 合并。

### 4.4 测试先行

以下逻辑优先先写失败测试：

- 浏览器权限申请、撤销和 API 失败；
- 书签移动、递归删除、URL 校验和根节点保护；
- 会话导入、恢复、失败跳过和自动快照保留；
- Outbox、幂等、版本冲突、墓碑、游标和重试；
- Supabase Auth、RLS、RPC 和数据库迁移；
- AI Schema、工具白名单、目标重校验、过期计划和用户确认；
- CORS、Token、敏感数据过滤和提示词注入。

禁止为了让测试通过而改变真实安全语义。

### 4.5 独立审查

实现完成后至少进行：

1. 规格审查：是否满足 Issue、验收标准和非目标；
2. 代码审查：是否存在越权、数据覆盖、循环同步、重复事实来源、并发、冲突、隐私和兼容问题。

审查必须重新读取 Issue、完整 diff、测试、迁移和安全协议，不能仅依赖实现阶段结论。

### 4.6 完成验证

完成前提供实际证据：

- `pnpm typecheck`、`pnpm test`、`pnpm build` 等实际结果；
- Chrome/Edge 扩展加载与关键交互验证；
- 失败路径、权限拒绝和数据边界；
- 真实 Supabase/PostgreSQL 迁移与 RLS 验证（适用时）；
- 回滚方法、尚未确认的假设和剩余风险。

未在真实 Supabase 项目验证的迁移、RLS 或 RPC，不得表述为已完成生产验证。

## 5. Git 与堆叠 PR

- `main` 是唯一正式主分支；
- 不直接修改或推送 `main`；
- 默认 squash merge，合并后删除功能分支；
- 不强制推送改写已公开审查历史，除非明确处理分支事故；
- 堆叠 PR 默认最多 2～3 层；
- 超过 3 层时优先审查并合并稳定基础层，不继续增加依赖层；
- 下游 PR 必须明确写出上游 base、依赖接口和尚未验证项；
- 上游发生结构性变化时，下游重新审查完整增量，不只处理冲突。

## 6. 浏览器与本地数据

- 原生书签以 `chrome.bookmarks` 为唯一事实来源；
- 云收藏与原生书签必须保持模型和 UI 边界，不进行未经设计的双向同步；
- 会话恢复默认新建窗口，不覆盖当前浏览环境；
- 自动快照必须有数量和空间上限；
- 导入文件必须经过结构、版本、大小和 URL 校验；
- 不可恢复的内部页面和危险协议不得被静默打开；
- 数据库升级必须保持已有本地数据可迁移或可明确回滚。

## 7. AI 安全边界

- Provider 输出一律按不可信 `unknown` 数据处理并严格校验；
- 模型不能直接调用 `chrome.*` API；
- 写操作必须使用白名单、结构化计划、风险重算和用户确认；
- 执行前重新核对标签、窗口、书签和 URL，拒绝过期计划；
- 标签标题、URL 和网页正文不能成为绕过策略层的指令；
- 扩展不保存模型服务密钥；
- 模型请求只发送完成任务所需的最小数据。

## 8. 云同步与服务端

- 不使用 service-role 绕过普通用户 RLS；
- 用户 Bearer Token、密码和模型 API Key 不进入同步实体；
- 变更必须幂等，冲突不得静默采用最后写入者获胜；
- 删除需要墓碑、传播和清理策略；
- Outbox 必须有失败次数、重试边界、手动恢复和可见状态；
- RPC、迁移和 RLS 必须在真实 PostgreSQL/Supabase 环境验证；
- 服务端不能获得不必要的浏览器权限或直接执行浏览器操作。

## 9. 兼容与完成标准

- 核心路径兼容 Chrome 109；
- 不依赖未经确认可用的高版本浏览器 API；
- 所有权限应按功能按需申请并可撤销；
- CI 通过、无冲突、无未解决审查线程只是合并条件，不替代真实浏览器和远端服务验证；
- 无法验证真实平台行为时，应明确停止条件和剩余风险，不得继续扩大实现。