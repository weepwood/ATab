{
  "name": "@atab/e2e",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "node -e \"console.log('E2E smoke tests are executed explicitly after the production build.')\"",
    "smoke": "playwright test",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.1",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.3"
  }
}
